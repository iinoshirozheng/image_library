name: Save Docker Images and Upload to Release

on:
  push:
    branches: [ "main" ]

jobs:
  save-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: 安裝 Docker
        uses: docker/setup-buildx-action@v3

      - name: 拉取映像
        run: |
          docker pull docker.io/library/redis:7.2
          docker pull registry.access.redhat.com/ubi8/ubi-minimal:latest
          docker pull registry.access.redhat.com/ubi9/ubi-minimal:latest
          docker pull registry.k8s.io/pause:3.9

      - name: 儲存為 .tar 文件
        run: |
          mkdir -p docker_images
          docker save docker.io/library/redis:7.2 > docker_images/redis-7.2.tar
          docker save registry.access.redhat.com/ubi8/ubi-minimal:latest > docker_images/ubi8-minimal.tar
          docker save registry.access.redhat.com/ubi9/ubi-minimal:latest > docker_images/ubi9-minimal.tar
          docker save registry.k8s.io/pause:3.9 > docker_images/k8s-pause.tar

      - name: 建立或更新 Release 並上傳檔案
        uses: softprops/action-gh-release@v1
        with:
          tag_name: docker-images-v1
          name: "Docker Images Release"
          files: |
            docker_images/redis-7.2.tar
            docker_images/ubi8-minimal.tar
            docker_images/ubi9-minimal.tar
            docker_images/k8s-pause.tar
          fail_on_unmatched_files: false
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
